class Response {
  constructor(params) {
    // Set up filters,
    // Define defaults
    // Keep callback around for execution
  };

  evaluate(data) {
    // Evaluate quality of data
    // Parse JSON if so needed
    // Set score
    // Result to original callback
  };
};
